<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <h1>@Model.GetText()</h1>
        <h4>Notes</h4>
        <ul>
            @foreach(var note in @Model.GetNotes())
            {
                <li><a href="#">@note.GetText()</a></li>
            }
        </ul>
        <form action="#" method="post">
            <label for="new-note">Add Note</label>
            <input id="new-note"type="text" name="new-note" autofocus>
            <button type="submit" name="button">Add Note</button>
        </form>
    </body>
</html>
